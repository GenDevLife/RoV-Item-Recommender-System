# app/config.py
import os

# หาตำแหน่งของโฟลเดอร์ app
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
# ชี้ไปที่ไฟล์ rov_data.db ที่อยู่หน้า Root
DB_PATH = os.path.join(BASE_DIR, 'data', 'rov_data.db')

# =========================================
# 🎯 Game Rules & Constraints
# =========================================
STATS_CAPS = {
    "cdr": 0.40,           # Cooldown Reduction (40%)
    "aspd": 2.00,          # Attack Speed (200%)
    "crit_rate": 1.00,     # Critical Rate (100%)
    "move_speed": 800      # Soft cap (ค่าสมมติ)
}

# =========================================
# ⚖️ Scoring Weights (ค่าคะแนนและบทลงโทษ)
# =========================================
PENALTIES = {
    "duplicate_passive": -50.0,  # โทษฐานออกของ Passive ทับกัน
    "boots_limit": -100.0,       # โทษฐานออกรองเท้าเกิน
    "jungle_wrong": -200.0,      # โทษฐานออกของป่าแต่ไม่มี Punish
    "support_limit": -100.0      # โทษฐานออกของซัพเกิน
}

# =========================================
# 🧬 Genetic Algorithm Settings
# =========================================
GA_SETTINGS = {
    "POP_SIZE": 50,        # จำนวนประชากรต่อรุ่น
    "MAX_GEN": 100,        # จำนวนรุ่นสูงสุด
    "MUTATION_RATE": 0.2,  # โอกาสกลายพันธุ์
    "ELITISM_COUNT": 2     # เก็บตัวเทพไว้ 2 ตัวเสมอ
}